Immadiate:

pausable unit movement
add A*

[DONE] pausable cursor
[DONE] seleting movepath via cursor without A*
[DONE] cursor movement while movable is visible


Eventually:



rather than just a "Units" blob in Game,
make Units have subsections representing "affiliations"
like "player", "enemy" etc

[DONE] implement a better queue that is linked
